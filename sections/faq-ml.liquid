{{ 'faq-ml.css' | asset_url | stylesheet_tag }}

{%- comment -%}
  Esta sección puede usar metafields del producto para mostrar FAQs específicas.
  Si no hay metafields, usa los bloques de la sección como fallback.

  Metafield esperado: product.metafields.custom.faq_items
  Tipo: list.metaobject_reference (con metaobject de tipo 'faq_item')
  Campos del metaobject:
  - question (single_line_text_field)
  - answer (multi_line_text_field)
{%- endcomment -%}

{%- assign product_faqs = product.metafields.custom.faq_items.value -%}
{%- assign has_product_faqs = false -%}
{%- if product_faqs != blank and product_faqs.size > 0 -%}
  {%- assign has_product_faqs = true -%}
{%- endif -%}

<div class="faq-ml section-{{ section.id }}-padding section-bg-{{ section.settings.background_style }}">
  <div class="page-width">

    {%- if section.settings.heading != blank -%}
      <h2 class="faq-ml__heading">{{ section.settings.heading }}</h2>
    {%- endif -%}

    {%- if section.settings.subtitle != blank -%}
      <p class="faq-ml__subtitle">{{ section.settings.subtitle }}</p>
    {%- endif -%}

    <div class="faq-ml__accordion">
      {%- if has_product_faqs -%}
        {%- comment -%} Mostrar FAQs del producto desde metafields {%- endcomment -%}
        {%- for faq_item in product_faqs -%}
          <div class="faq-ml__item">
            <button
              class="faq-ml__question"
              type="button"
              aria-expanded="false"
              data-faq-toggle
            >
              <span class="faq-ml__question-text">{{ faq_item.question.value }}</span>
              <svg class="faq-ml__icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                <path class="faq-ml__icon-horizontal" d="M5 10h10" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                <path class="faq-ml__icon-vertical" d="M10 5v10" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
              </svg>
            </button>
            <div class="faq-ml__answer" data-faq-answer>
              <div class="faq-ml__answer-content">
                {{ faq_item.answer.value | newline_to_br }}
              </div>
            </div>
          </div>
        {%- endfor -%}
      {%- else -%}
        {%- comment -%} Mostrar FAQs de los bloques de la sección (fallback) {%- endcomment -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'question' -%}
            <div class="faq-ml__item" {{ block.shopify_attributes }}>
              <button
                class="faq-ml__question"
                type="button"
                aria-expanded="false"
                data-faq-toggle
              >
                <span class="faq-ml__question-text">{{ block.settings.question }}</span>
                <svg class="faq-ml__icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
                  <path class="faq-ml__icon-horizontal" d="M5 10h10" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                  <path class="faq-ml__icon-vertical" d="M10 5v10" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                </svg>
              </button>
              <div class="faq-ml__answer" data-faq-answer>
                <div class="faq-ml__answer-content">
                  {{ block.settings.answer }}
                </div>
              </div>
            </div>
          {%- endif -%}
        {%- endfor -%}
      {%- endif -%}
    </div>

  </div>
</div>

<script>
(function() {
  const faqToggles = document.querySelectorAll('[data-faq-toggle]');

  faqToggles.forEach(toggle => {
    toggle.addEventListener('click', function() {
      const item = this.closest('.faq-ml__item');
      const answer = item.querySelector('[data-faq-answer]');
      const isExpanded = this.getAttribute('aria-expanded') === 'true';

      // Cerrar todos los dem�s items
      faqToggles.forEach(otherToggle => {
        if (otherToggle !== toggle) {
          const otherItem = otherToggle.closest('.faq-ml__item');
          const otherAnswer = otherItem.querySelector('[data-faq-answer]');
          otherToggle.setAttribute('aria-expanded', 'false');
          otherItem.classList.remove('is-active');
          otherAnswer.style.maxHeight = '0';
        }
      });

      // Toggle el item actual
      if (isExpanded) {
        this.setAttribute('aria-expanded', 'false');
        item.classList.remove('is-active');
        answer.style.maxHeight = '0';
      } else {
        this.setAttribute('aria-expanded', 'true');
        item.classList.add('is-active');
        answer.style.maxHeight = answer.scrollHeight + 'px';
      }
    });
  });
})();
</script>

{% schema %}
{
  "name": "Preguntas Frecuentes ML",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "T�tulo",
      "default": "Preguntas frecuentes"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subt�tulo"
    },
    {
      "type": "header",
      "content": "Estilo de secci�n"
    },
    {
      "type": "select",
      "id": "background_style",
      "label": "Estilo de fondo",
      "options": [
        {
          "value": "white",
          "label": "Blanco"
        },
        {
          "value": "gray",
          "label": "Gris claro"
        },
        {
          "value": "orange-light",
          "label": "Naranja suave"
        }
      ],
      "default": "white"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Espaciado superior",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Espaciado inferior",
      "default": 60
    }
  ],
  "blocks": [
    {
      "type": "question",
      "name": "Pregunta",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Pregunta",
          "default": "�Pregunta ejemplo?"
        },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Respuesta",
          "default": "<p>Respuesta ejemplo</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Preguntas Frecuentes ML",
      "settings": {
        "heading": "�Con qu� telef�nos es compatible?"
      },
      "blocks": [
        {
          "type": "question",
          "settings": {
            "question": "�Ten�is una tienda a Blanconita?",
            "answer": "<p>S�, tenemos tienda f�sica en Blanconita. Puedes visitarnos de lunes a s�bado de 9:00 a 18:00.</p>"
          }
        },
        {
          "type": "question",
          "settings": {
            "question": "�D�nde puede usar esto fondos?",
            "answer": "<p>Puedes usar nuestros productos en cualquier lugar. Son compatibles con la mayor�a de dispositivos.</p>"
          }
        },
        {
          "type": "question",
          "settings": {
            "question": "�Qu� tan seguro es?",
            "answer": "<p>Muy seguro. Todos nuestros productos pasan por estrictos controles de calidad y cuentan con garant�a.</p>"
          }
        },
        {
          "type": "question",
          "settings": {
            "question": "�Qu� dice contiene en?",
            "answer": "<p>El paquete incluye el producto principal, manual de instrucciones y accesorios necesarios.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
